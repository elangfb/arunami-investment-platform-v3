# Mizan Memory

> Slim catch-all for cross-cutting caveats/gotchas with no better home. **One line each** —
> not status, not history, not a write-up.
> Add only if: no other layer owns it, it stays useful next session, a future agent may repeat a
> costly mistake without it, and it fits on one line.
> If it needs more than a line, it belongs in a design/decision/guide/session — link there, don't expand here.
> Standing "always do X" preferences are always-on context → `AGENTS.md`, not here.
> Prune aggressively: delete the moment an entry goes stale or earns a canonical home.
> The external auto-memory store is intentionally empty — it only redirects here.

- 2026.06.03 — `docs/reconcile` — Reconciling two diverging knowledge sources: decide authority **per-topic by git date × nature** (built behavior → the live repo is reality; unbuilt design/domain → newest commit wins, even the "lesser" repo) — never by which repo is canonical. → `sessions/2026.06.03-brainstorm-merge-and-gate/02-which-wins-recency.md`
- 2026.06.04 — role/desk fold + Rapat (ADR-0005): `Role` folded to SOP `RM | LG | RA | CM | MG` (AO+LA→RM, RT→RA); desk codes renamed to functional names; seed grants by per-user `roleKey` (not `Role`), `ownersForStage` is **desk-based**; Stage enum stays 1–6 (4-phase = **derived** `phaseOf`, not an engine renumber); Komite has **no in-app voting** (chair records outcome + attending Komite QR-sign per-app MoM). Detail → `decisions/0005-rapat-komite-signed-minutes.md`, `GLOSSARY.md`, `apps/web-app/src/lib/{types,desks}.ts`.
- 2026.06.04 — SeaweedFS `latest` no longer auto-creates an S3 bucket on `PutObject` (returns `AccessDenied`, not `NoSuchBucket`); each `*.itest.ts` runs in its own `node --test` worker, so any file touching object storage must call `ensureBucket()` in `before` itself — don't rely on another itest creating it. Pattern: `apps/web-app/src/server/{docs/checkpoint,storage/documents}.itest.ts`.
- 2026.06.05 — A `sticky top-0` element inside the AppShell scroll container (`AppShell.tsx`, padding `p-4`/`md:p-6`) pins at the **content-box** edge (below the padding), not the visual top — leaving a padding-tall gap above it that content bleeds through while scrolling. Negative margin alone only hides it at scroll 0; the sticky `top` must ALSO be negative and mirror the padding (`-top-4`/`md:-top-6`). Pattern: `components/layout/ImpersonationBanner.tsx`.
- 2026.06.05 — Auth Emulator login 401 (`auth.session_create_failed`, Prisma unique on `email`): when `.emulator-data` is regenerated the emulator re-issues NEW localIds, so a previously-linked `User.firebaseUid` goes **stale** and re-login fails. `ensureUser` (`server/repo/users.ts`) step 2 now ADOPTS an existing row by email even when it holds a stale uid (re-points `firebaseUid`), not only when null — verified email is the stable identity. Stale rows self-heal on next login; no manual repair. Guard: `users.itest.ts`.
- 2026.06.05 — agent-browser walkthrough: Firebase Auth emulator account chooser (`.js-reuse-account` `<li>`) does NOT advance on agent-browser's CDP coordinate `click` (mdc-ripple swallows it) — dispatch a native `el.click()` via `eval --stdin` instead; the popup then closes and the app tab navigates to `/dashboard`. Launch headed Helium with `--executable-path /opt/helium-browser-bin/helium` (the raw binary, not `helium-wrapper`, which munges stdio). Demo roster + superadmin: `src/lib/seed-data/demo-logins.ts`.
- 2026.06.05 — agent-browser `eval` runs in a **persistent** VM (the daemon holds it across calls AND across separate CLI invocations), so a top-level `const x` survives and a later `const x` throws `Identifier 'x' has already been declared`. Wrap every `eval --stdin` body in an IIFE `(() => { … return …; })()` so declarations are block-scoped and scripts stay re-runnable. Reusable replay: `scripts/walkthrough.sh` (step-gated; `AUTO=1` hands-free).
- 2026.06.05 — agent-browser multi-account scripting: `click` accepts ONLY CSS / plain XPath (`//…`) / snapshot `@eN` refs — `text/`, `aria/`, `xpath/` selectors fail "Element not found"; use `find role button click --name "…"` for semantic clicks. The Google login button MUST be a **trusted** CDP click (`signInWithPopup`'s `window.open` is popup-blocked for native `eval` clicks). Switch persona = `GET /api/auth/logout` (clears cookie → `/login`, never auto-restores) → `find role button --name "Masuk dengan Google"` → native-pick `.js-reuse-account` by email in the auto-focused popup (tab id varies — it auto-focuses; app tab stays `t1`). `scripts/walkthrough.sh` does all 10 role personas (`SOLO=1` to pin one).
- 2026.06.07 — dev "Autofill" button is removed (`components/dev/` deleted; the F3 hit-testing trap is gone). End-to-end sample data now lives in `resources/dummy-data/<persona>/` — regenerate with `pnpm gen:dummy` from the SSOT `apps/web-app/src/lib/dummy-data/personas.ts` (cheat-sheets + a real text-layer PDF per required doc, gate values authored to pass DSR≤40/LTV≤70/Kol≤1, guarded by `personas.test.ts`). Gotcha: the default `OCR_PROVIDER=stub` FABRICATES gate values from app data and ignores the uploaded bytes — the PDF content only matters under a real provider (`documentai`/`gemini`); the cheat-sheet values are always authoritative for the tester.
- 2026.06.08 — Google Docs `replaceAllText` is **exact-match (no regex)**: when de-customizing a filled legal doc into a template, a customer-name mention carried a stray private-use glyph (`U+E907`) between `PT` and the name (plus whitespace variants), so the clean full-string match silently missed 4 of 6 occurrences. Always dump the exact bytes around each marker, run a **denylist scan that fails on any surviving marker** before shipping a de-customized template, and watch for double-`PT` when the replacement token already includes the entity prefix. Pattern: `apps/web-app/scripts/author-momsp3-masters.ts`; design `docs/designs/document-system.md`.
- 2026.06.07 — A responsive **dual-render** (desktop `<table className="hidden md:block">` + a `<md` card reflow `className="md:hidden">`, e.g. the Pipeline mobile reflow in `PipelineTable.tsx`) keeps BOTH copies in the DOM, so a Playwright `getByText('…')` resolves to **2 nodes → strict-mode violation**. Use `.first()` in e2e assertions on any text that appears in both layouts. Fix pattern: `apps/web-app-e2e/steps/create-application.steps.ts`.
- 2026.06.07 — `pnpm test:e2e` boots its OWN `nx dev web-app` on `:4200`; if `pnpm dev` (the `:3000` dev) is already running, the e2e's nx dev **blocks on the nx `dev` target lock** held by the running dev and times out (log: `Waiting for @mizan/web-app:dev in another nx process`). Kill the dev server first (`pkill -f "next-server"; pkill -f "nx dev web-app"`) before running e2e; a `setsid`-spawned dev can also orphan a `:4200` server that keeps holding the lock. Harness: `scripts/test-e2e.sh`.
- 2026.06.08 — docs drift — a structural rename/fold (roles/desks/stages/enums) leaks stale refs FAR beyond the canonical layer: build sessions update only `GLOSSARY`/`types`+plan+MEMORY and leave CURRENT-STATE, both `AGENTS.md`, guides, designs, references, and the `mizan-design` skill stale (caught 2026.06.08, role/desk fold + signed-MoM Rapat). After any fold, run a cross-layer `tidy-knowledge` sweep — fix live docs, add a one-line reconcile banner to domain refs (pattern `references/akad-types.md:8`), and leave the historical layer (ADRs, REALIZED designs, personas, SOP digest, merge records) frozen. Applies next to the deferred engine 6→4 stage renumber (~158 `.stage===N` sites).
- 2026.06.09 — Vertex inference dies **silently** (fail-open → empty MUAP narrative AND web-research everywhere, no visible error) when misconfigured: `GOOGLE_CLOUD_PROJECT` unset → throws "AI not configured"; or `GOOGLE_CLOUD_LOCATION` names a region that doesn't serve the model. Proven for `gemini-3-flash-preview`: `asia-southeast2`(Jakarta)→"Precondition check failed", `asia-southeast1`(SG)/`us-central1`→404, only **`global`** serves it (and `global` egresses to US). `gemini-2.5-flash` IS served in `asia-southeast1` if an APAC-clean GA model is wanted. Both local `.env.local` (was Jakarta) and server `~/mizan/.env` (project+location BOTH missing) were broken → this is THE reason "AI develops nothing / drafts empty" was felt repo-wide. Test in isolation with `@google/genai` (vertexai:true, decode base64 `VERTEX_CREDENTIALS`) before blaming app code. Defs: `server/ai/gemini.ts` `resolveVertexLocation`/`vertexGoogleAuthOptions`.
- 2026.06.09 — Server (aixel) research stack: `compose.shared.yaml` searxng binds `127.0.0.1:8080:8080`, but host `:8080` is taken on the server → searxng fails to start, **detaches from `mizan_internal` (no IP)** → `mizan-web`'s `http://searxng:8080` search silently dies → research returns `[]`. Fix: recreate searxng with `ports: !override []` (no host port needed — mizan-web reaches it by service DNS). To dev LOCALLY against the server stack (firecrawl OSS is RAM/CPU-heavy — worker logs "Can't accept connection due to RAM/CPU load" and rejects scrapes under load → intermittent `[]`): `scripts/research-tunnel.sh` (auto-discovers searxng container IP, forwards local `8089→searxng`/`3002→firecrawl`; `down` to close). Set `WEB_RESEARCH_PROVIDER=searxng-firecrawl` + `SEARXNG_URL=http://localhost:8089` + `FIRECRAWL_URL=http://localhost:3002` in `.env.local`.
- 2026.06.11 — `createApplicationForActor` (`server/actions/application-create.core.ts`) mints the `FOS-2026-NNN` display id from `prisma.application.count()+1` — a **non-atomic read-then-write**: two concurrent creates collide on the id PK (P2002). Now wrapped in a **bounded retry-on-P2002** (recompute id offset by attempt; customer resolution stays outside the loop). This surfaced as a parallel-`node:test` itest flake (each `*.itest.ts` is its own worker; several now create apps), not just a theoretical prod race. When adding an itest that creates apps, expect concurrency on this path — the retry covers it; don't `migrate reset`.
- 2026.06.11 — Doc-discovery matcher (`lib/doc-discovery/matcher.ts`, P2 Topic 3) matches aliases as **word-boundary substrings** (flanked by non-alphanumerics or path start/end), NOT raw substring — a refinement of the design's "substring" wording that kills the short-abbreviation false-positive class (`IMB`↛"Timbangan", `KK`↛"OKK"). A false ✅ silently satisfies a checklist requirement (worse than a ⬜ the RM fixes by renaming); smushed names with no separator fall to the RM-renames path. Generic whole-word aliases still need curation (dropped bare `Sertifikat`/`Kontrak`/`Asuransi`; disambiguated rekening koran pribadi/perusahaan in `aliases.ts`). (Real Google Drive client + reconciliation UI + OCR-widening since shipped — see `CURRENT-STATE.md` P2.)
