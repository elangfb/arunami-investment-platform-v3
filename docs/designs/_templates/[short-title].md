# <Design title>

- **Status:** Draft (linked to active plan or review date) | Current | Superseded by <path>
- **Last reviewed:** YYYY.MM.DD

> A durable **design contract / blueprint** — how a feature or subsystem is built, and the
> conventions to follow when working in it. NOT a plan (active work lives in
> `docs/planning/`) and NOT an ADR (the *why we chose* is `docs/decisions/`). Drafts must link an active plan or carry a review/exit date.

## Overview

<what this designs, and the shape of the solution in a few lines.>

## Design

<the architecture / data model / contracts / state machine. Link to real code paths
(`apps/web-app/src/…`) instead of restating them.>

## Conventions & invariants

<rules that must hold when working in this area; what callers must never do.>

## Open questions / deferred

<known gaps, or a pointer to the active plan / deferred register. Do not use this as a task tracker.>
