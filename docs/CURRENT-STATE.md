# Current state

> A live snapshot of what's true / built right *now* — updated as things ship.
> NOT a chronological log (that's git), NOT active plans (`docs/planning/`), and NOT a roadmap/backlog.
> Keep it lean; **link** to details rather than restating them.

- **Updated:** 2026.06.12 — RM-led redesign P1–P5 **merged to `main`** (PRs #1/#2/#5, 2026.06.12). Residuals tracked in [`planning/execution-queue.md`](planning/execution-queue.md) (live-demo smoke, dedup migration, prod posture).

## Live facts

- **App platform** — done — PostgreSQL via Prisma; `server/repo/*` reads, all writes via `server/actions/*`. `src/lib/seed-data/` is seed-source only. → `guides/architecture.md`
- **Customer entity + customer-first entry (ADR-0020, P1)** — done 2026.06.11 — first-class `Customer` (`individual|business`; identity columns + Zod-JSON `pengurus[]`/`pemegangSaham[]`); **additive + dual-read** migration (Application keeps its identity columns as the read source; 1:1 backfilled, parity 47/47/47/0) so no reader changed. **Dual-write** on create + update (`application-create.core.ts`; `write.ts mirrorIdentityToCustomer` MERGEs, never clobbers a shared Customer). **Customer-first UI:** `/nasabah` list + `/nasabah/[id]` detail (open-read; carry-forward of a customer's applications); "Pengajuan Baru" launches an app inside the Nasabah file (`customerId` link-direct, no dedup fork). **Create-time soft dedup nudge** (advisory, non-blocking; individual NIK + business NPWP/NIB; identity compares as trimmed strings, never `Number()`). Proof: typecheck · test (unit 450/450, integration 79/79); `live-demo` pending. **Deferred (human-gated):** the partial-unique-index dedup migration (1 existing dup-NIK group must be deduped first — `planning/execution-queue.md` item 3). → `decisions/0020-customer-entity-and-rm-led-pipeline.md`
- **Document discovery + Drive (ADR-0020, P2)** — done 2026.06.11 — RM drops source docs in Google Drive (any layout); Mizan reconciles them to the checklist. **Deterministic word-boundary matcher** (`lib/doc-discovery/matcher.ts`, admin-editable aliases, many-to-many, full-path, no AI/OCR — "discovery never reads content") → two checklist cards **Dokumen Nasabah / Dokumen Pengajuan** with 3 states ✅/⬜/⚠️ (`DocumentDiscoveryPanel`). **Real Google Drive provider** (`server/discovery/google.ts`, `DRIVE_PROVIDER=google`, recursive `files.list`, md5 content-address; **live-verified**) + a **stub** seam (default for dev/test). Per-app/customer folder refs (`Customer/Application.driveFolderId`); RM links a folder URL, "Pindai ulang" rescan, opt-in scaffold (warn on missing Editor). **Manifest version ledger** (`SourceDocManifestEntry`, refs+sha256, dedupe-by-sha256, no byte copy — Fork B5). **OCR-widening:** 8 advisory fields + 3 cross-checks (SPT↔LapKeu, Akta↔Customer aggregate, identity↔customer-master) — **advisory only, NIK stays the sole blocker** (`advisoryExtractions` store, off the gating spine). Proof: typecheck · test (unit 490, integration 94) · Drive `live-demo`. → `designs/rm-led-pipeline-redesign.md` §3, `decisions/0020-customer-entity-and-rm-led-pipeline.md`
- **RM-led pipeline surface + gating (ADR-0020, P3)** — done 2026.06.11 — the redesign's pipeline layer, all over the unchanged `stage` Int (Fork A1, no authority inversion). **Spine:** 5-segment handoff display (Inisiasi → Risk Review → Komite → SP3 → Pencairan) + parallel checklist streams (`lib/pipeline-spine.ts`/`PipelineSpine`, display-only); Originasi→Inisiasi label. **⚠️ Gating relocation (REGULATORY):** the intake hard gates (required docs · intake/NIK OCR · NIK-mismatch · AML) moved from the 1→2 advance to the **MUAP→Risk** handoff (`muapToRiskBlockers`) — Inisiasi (stages 1–3) flows free, desk windows phase-wide; compliance proven (a deal cannot reach Risk Review without all gates + Legal/Appraisal). **Colek:** in-app desk-to-desk work request (`DeskAssignment`; resolver fewest-active-deals + tie→least-recently + sticky; complete/reject/reassign; derived notifications; Home + Alur-kerja). **Structured deliverables:** Analisa Yuridis `{opinion,catatan[]}`, Penilaian `{nilaiPasar,nilaiLikuidasi,penilai,tanggalLaporan}` (+ advisory cross-check), structured AML `{result,screenedParties[]}` + `originType` hook — *completion gates, the verdict doesn't*. **Explicit Generate-MUAP:** MUAP minted on an RM action (not auto; `muapDocId` nullable; ladder blocked until the doc exists). **SP3 Legal chain:** single-reviewer approval-chain (no stage advance) + disbursement dual-prerequisite (`sp3FinalReady` = Stage-6 + SP3-Legal-approved; ADR-0005 §4 routing untouched). Proof: typecheck · test (unit 530, integration 105). → `designs/rm-led-pipeline-redesign.md` §1/§4, `decisions/0018`/`0020`
- **AI context + access (ADR-0019, P4)** — done 2026.06.11 — **Layered AI context** (Topic 5): `Customer.contextMd` (≈root AGENTS.md) + `Application.contextMd` (≈app-local), each a live-derived AUTO block (`buildSeedContext` + prior-apps + `originType` framing) + a sacred human "Catatan" (the only stored part), rendered compact broad→narrow at the end of the user prompt. **Per-surface policy** (`lib/ai-context-policy.ts`): narrative/advisory/assistant/bureau/discussion → all 3 layers; research → customer-only (masked with real secrets); **`extract` → NONE** (cross-deal-contamination rule, test-pinned). contextMd editors are open+attributed (Fork A3). **`PII_MASK_ENABLED` flag** (Fork B4, default **OFF** in dev): `maskForEgress` is a raw pass-through when off (compliance parked), masks as before when `'1'` — machinery intact, re-enabled at W1. **Access** (Topic 6): open-read/scoped-write already in place (universal `canViewDoc`, scoped maker-checker writes); **generated docs Mizan-owned** (`mizanDocFolderId`) + a shortcut into the user's Drive folder + "Coba lagi" retry on missing Editor; grant ledger + freeze-on-advance preserved. Proof: typecheck · test (unit 552, integration 113). → `designs/rm-led-pipeline-redesign.md` §5/§6, `decisions/0019-open-read-scoped-write-access.md`
- **Drive root-folder share (ADR-0019 §3 V1)** — done 2026.06.11 — broad Drive READ via ONE root **"Mizan"** folder (`DriveRef` `mizan-root`): every **ADMITTED** user (superadmin or ≥1 effective desk — same boundary as the awaiting-access wall) gets one per-email `reader` grant on the root (`DriveRootGrant` ledger; `role 'invalid'` = permanent-rejection marker); per-app `mizanDocFolderId` folders parent under the root (inheritance ⇒ N grants instead of N×M). Converges at login + admin grant/revoke (`syncRootGrantForUser`, non-blocking `after()`, offboarding revokes) + ops sweep `scripts/reconcile-drive-root.ts` (backfill · revoke-down · trust-but-verify `permissions.list` diff · MOVE-reparent of legacy flat folders — Drive v3 single-parent). Per-doc JIT writer grants + freeze-on-advance unchanged. ~600 direct-permission ceiling (alarm at 500) forces the W1 group/domain swap (needs Workspace org). Proof: typecheck · test (unit 562, integration 127) · **live-demo** — `reconcile-drive-root.ts` ran against real Drive 2026.06.11: created the real root **"Mizan"** folder (`DriveRef` `mizan-root`), granted the 2 real admitted accounts (silent — `sendNotificationEmail:false`), 18 `@example.com` seed actors landed `invalid` (`403 cannotInviteNonGoogleUser`, expected), 0 failed / 0 reparented (no legacy flat folders existed). Note: each future dev sweep re-attempts those 18 seed `invalid` rows — harmless noise, gone once the seed admitted set is real. → `server/docs/root-share.ts`, `decisions/0019-open-read-scoped-write-access.md` §3-addendum
- **Review / adendum lifecycle (P5, Topic 7)** — done 2026.06.11 — an approved/disbursed facility can be **reviewed** (Bank-initiated) or **amended** (adendum, Nasabah-initiated), both reusing the FULL pipeline. `Application.sourceApplicationId` (self-relation) forms an ordered **lineage**; `getLineage` walks to the root (cycle-guarded); the head = "ketentuan terkini" (`FacilityLineageCard`). "Mulai Review"/"Buat Adendum" CTAs (DetailCockpit, intake-gated) → `startReview/startAdendumAction` create a child via `createApplicationForActor` (carry terms + same Customer, `originType` set, **`amlAttestation` starts null → fresh attest required, no gate change**). **Cadence** (`lib/review-cadence.ts`): cascade default 12mo → `Customer.reviewCadenceMonths` override (facility deferred, C7); `reviewDueState` anchors on `Application.disbursedAt` (set at Cair) — reads **only dates, never any payment/Kol** ("Mizan records, never monitors"); derived "review jatuh tempo" notification. Proof: typecheck · test (unit 562, integration 117). → `designs/rm-led-pipeline-redesign.md` §7
- **6-stage pipeline (workflow fine-tune)** — done — core shipped 2026.05.28. → `designs/workflow-finetune.md` §0, `guides/workflow.md`
- **Maker-checker + Rapat Komite + role/desk fold** — done 2026.06.04 — append-only `ApprovalStep` ladder + pure reducer (`lib/approval-chain.ts`); ladders **two-rung**: MUAP `RM→Team Leader` & RSK `RA→Risk Team Leader` with QR sign/verify/stamp — TL/RTL approve freezes the doc; DPS keeps only the Stage-5 `dps-review` gate, no longer an RSK signer (shipped 2026.06.12 — typecheck+unit+integration verified; live smoke pending; [`decisions/0021-two-rung-approval-chains.md`](decisions/0021-two-rung-approval-chains.md) supersedes ADR-0003 §Decision.2); **awaiting-signature push (2026.06.09):** checker rungs (not stage owners) see pending signatures on Beranda (`AwaitingSignaturePanel`) + sidebar badge + `/notifications`, all derived from `listAwaitingApprovalNotices`; **STRICT per-submitter routing shipped 2026.06.09** (`lib/approval-routing.ts` + `server/config/approval-routing.ts`) — only the designated account may sign a configured rung, unconfigured rungs fall back to all-holders, plus a signer Home "Tugas Saya" TODO + an `approval` notification; separates **authority** (desk grant + four-eyes) from **routing** (admin config), production map is W1 (`designs/admin-config-layer.md`); Lane-B documents (MUAP/RSK fill on live masters — V2 NamedRange then, **now V3 `replaceAllText`**, see below; Markdown read-back, MoM/SP3 generation); **Rapat Komite (ADR-0005)** — no in-app voting, chair records per-app outcome + attending Komite QR-sign the per-app MoM (≥2 quorum), routing on all-signed, involved-team participant attestation (non-blocking); **role/desk fold** (AO+LA→RM, RT→RA; functional desk codes, seed grants by `roleKey`, `ownersForStage` desk-based); **4-phase derived view** (`phaseOf`/`PHASE_NAMES`). Built on the working 6-stage engine; verified unit/integration/e2e. → `designs/workflow-target.md`, `designs/workflow-engine.md`, `decisions/0005-rapat-komite-signed-minutes.md`
- **MUAP/RSK docs** — **V3 wired (core facts) + QR anchors done** — `createApplicationDocs` fills via the V3 registry (`seed.ts` `fillApplicationDoc`: unique-placeholder `replaceAllText`, value-or-original-placeholder → **no `{{}}` leak**). RAW V3 templates authored for the core facts (company/plafond/terbilang/tenor/agunan/dates); real-doc smoke verified. V1/`seed-v2` deleted. Signature NamedRange QR anchors created on the masters; the two-rung chains (ADR-0021) stamp MUAP `tanggal_ttd_{rm,tl_spv}` + RSK `rsk_sig_analyst_tanggal` via `stampSignatureQr`, and the RTL slot `rsk_sig_rtl_tanggal` stamps as a safe no-op until the template owner updates the RSK master signature block (the masters still carry the legacy `bm_ku`/`officer`/`cro`/`dps` anchors). Narratives are N/A (the real templates use granular human-fill prompts, not 5C+1S AI slots). → `designs/document-system.md`, `references/document-templates.md`, `decisions/0013-docs-generation-v3-replace-all-text.md`
- **MoM/SP3 docs** — **rebuilt from the real reference docs (2026.06.08)** — old from-scratch templates replaced by V3 masters de-customized from the real filled reference docs: Mizan-known fields → `{{token}}` (existing `buildMomFill`/`buildSp3Fill` names, no builder change), customer data → `[human placeholder]`, generic legal boilerplate kept; per-case MoM committee tables emptied to a fillable grid. Authored + denylist-scanned (leak-free) by `scripts/author-momsp3-masters.ts`; smoke-tested end-to-end. Env `GOOGLE_{MOM,SP3}_TEMPLATE_DOC_ID` repointed. → `designs/document-system.md`, `references/document-templates.md`
- **OCR → narrative** — done — full-text OCR feeds MUAP/RSK narrative; KTP excluded. Prod needs `OCR_PROVIDER` env (code default = stub). → `guides/document-ai-ocr.md`
- **Config & admin foundation** — done — hardcoded → admin-editable versioned config + 3 admin desks (USERS/MASTER/POLICY) + real OCR (Phases A–D). Remaining config extensions (required-doc checklist, risk-policy audit display, broader OCR coverage, branch/region scope) stay in `planning/config-and-admin.md`. Compliance masking = bracket+regex, no NER. **Superadmin is workflow-read-only (ADR-0010):** effective desks = ADMIN-* + MG observer (no pipeline power); acts on the workflow ONLY via impersonation (audited "a.n. Superadmin"); impersonation stays superadmin-only; footer "Akhiri" end-impersonation button complements the sticky banner. → `decisions/0010-superadmin-read-only-acts-via-impersonation.md`
- **Analysis (5C+1S)** — done — app-canonical, AI fills (Gemini), deterministic server-recomputed scores; **DB-persisted** (`analysis` Json column, round-trip tested in `repo/write.itest.ts`); locked on stage move.
- **AML attestation gate (relocated to MUAP→Risk by the RM-led redesign, 2026.06.12)** — RM (`intake`) attests the EXTERNAL Initial-AML check (DTTOT/PEP/negative-list) PASSED; MIZAN performs NO screening. **Since the redesign, the intake hard-gates (docs + intake-OCR/NIK + AML) no longer gate Stage 1→2** (`stage1To2Blockers` returns `[]`); they compose at the **MUAP→Risk submit** (`muapToRiskBlockers`, enforced at the MUAP ladder request in `server/actions/approval.ts`), settable across Inisiasi (stages 1–3). Attestation resets on 2→1 / 3→1 send-back. Audit via `appendHistory`. → `lib/aml.ts`, `lib/stage-action.ts`, `server/actions/application-stage.ts` (see also the P3 entry above)
- **AI inference provider** — **Vertex-only, live** (cutover shipped 2026.06.09). Gemini Flash via the Vercel AI SDK (`server/ai/provider.ts`, `server/ai/gemini.ts`) on Vertex (`aiplatform.googleapis.com` on `hijra-mizan`, SA `mizan-vertex@hijra-mizan` `roles/aiplatform.user`, `GOOGLE_CLOUD_PROJECT` + `VERTEX_CREDENTIALS`). The AI Studio (`GEMINI_API_KEY`) path is **removed** (no silent-precedence footgun). The residency guard is **warn-only, not fail-closed**: out-of-region locations **including `global`** are permitted; out-of-APAC logs `ai.region_out_of_apac` (`isApacLocation`/`resolveVertexLocation`) but never blocks. ⚠️ **Compliance:** `global` egresses Bank PII outside APAC (US) — reverses the OJK §27 fail-closed guard; accepted **interim**, revisit before production / Dec-2026. → `references/compliance.md`
- **Committee decision note** — done 2026.06.03 (action renamed in the Rapat redesign) — the chair-set outcome (`setKomiteOutcomeAction`, formerly `submitDecisionAction`) server-enforces a mandatory note on Conditional/Reject (`validateDecisionNote`), mirroring the AML/risk-note discipline. → `decisions/0005-rapat-komite-signed-minutes.md`
- **SLA engine** — done — per-stage day-targets (versioned config) + **additive per-DESK business-day (HK) targets** (`deskSlaState`, falls back to per-stage when unconfigured → behavior-preserving) + Asia/Jakarta **business-calendar** helpers (`businessDaysElapsed`, `isWithinBusinessHoursJakarta`). **Holiday calendar — done 2026.06.10** — `isJakartaHoliday` wired to a real **bundled calendar + admin-set overrides + public-holiday-API fetch**, merged into the SLA business-day clock (`server/config/holidays.ts`, `lib/scheduling/jakarta-clock.ts`; managed in the admin Master tab). Exact per-desk values + per-task clock-start triggers are W1. → `lib/sla-utils.ts`, `lib/scheduling/jakarta-clock.ts`, `lib/config/sla-policy.ts`
- **Bureau bundle (Stage 2)** — done 2026.06.03 — SLIK + **Pefindo** (`pefindo_report`, advisory) + Rek Koran uploadable/stored; **masked + audited AI bureau summary** (`server/ai/bureau.ts`, surface `'bureau'`, residual backstop fail-open by default / `PII_RESIDUAL_BLOCK=1` to fail closed) shown in the Data tab. Advisory only — Kol/gating stay human-confirmed + deterministic. → `lib/bureau-summary.ts`
- **PII residual-backstop policy** — done 2026.06.04 — masking ALWAYS runs on every AI text egress via the shared seam (`server/ai/redact.ts` `maskForEgress`); the `detectResidualPii` backstop is now **policy-configurable** (`blockOnResidualPii`/`PII_RESIDUAL_BLOCK`). **Default = fail-OPEN** (log `pii.residual_detected`, still egress — human-directed to keep Mizan presentable); `PII_RESIDUAL_BLOCK=1` restores fail-closed (required before real customer data — G2/G5). Same batch closed a gap: web-research **synthesis corpus** now masks before egress + audit (previously the only unmasked text-egress path). → `designs/pii-masking.md`
- **Required-docs checklist** — done — conditional builder + **purpose dimension** (`financingPurpose`: modal-kerja/pembangunan → RAB/Kontrak-SPK-PO/bouwheer; inert until intake captures it at W1) + badan-usaha + collateral (PBB) additions. 📝 New docTypes are NoEffort defaults pending W1. Bank-template hosting deferred (needs Bank artifacts). → `lib/required-docs.ts`
- **Komite cadence + MOM** — done 2026.06.03 — Mon/Wed/Fri cadence is pure config (3 schedule templates; materializer already handles N weekly slots). **Minutes-of-Meeting** artifact + **≤H+1 business-day SLA** (`meetingMomSlaState`) → notifications (`buildMeetingNotifications`); chair-only `recordMeetingMinutesAction`. ⚠️ Exact times/format W1. → `lib/komite.ts`, `server/actions/komite.ts`
- **UI system** — done — Refined Navy + IBM Plex tokens; shared Page shell (all 7 top-level pages); detail-page coherence (DossierSection); Komite decision system; Portofolio + Notifications rethink. **Detail page = adaptive RM-coordination command-center (ADR-0009):** Ringkasan `Alur kerja` worktable (`CoordinationPanel` / `lib/workstreams.ts` — parallel + do-it-early streams, navigates only, reuses engine predicates), unified MUAP/RSK doc-tab spine, hard-gate-when-assessable. Grouped nav stays always-visible (collapsible disclosure was reverted per user). **App-wide consistency spine (ADR-0011, full-Mizan Wave 1):** one `StatusChip`/SLA vocabulary (WCAG), Bahasa-chrome sweep (banking terms kept English), role-scoped "Tugas saya" + role-tagged owners, `EmptyState`/`ConfirmDialog`, mobile Pipeline reflow, `useActor` cutover (useRole shim deleted). Wave 2 = Home auto-assignment (kept Kanban; see next line), NOT the cockpit reshape. (The full-Mizan UI/UX review plan was retired in the planning slim-down; its durable UI facts now live in the `mizan-design` skill + `guides/detail-page.md`.) → load `mizan-design` skill, `guides/detail-page.md`
- **Home auto-assignment (ADR-0012, Wave 2)** — done — Beranda Saya stays a Kanban (Jira-familiar); apps auto-assign to users by REAL effective desk grants (role ∪ direct), not the seed — so each user's Home shows the apps assigned to them (admin-granted users included). Grant resolver injected through `dispatch`→`applyDecision` (seed fallback for tests). → `lib/stage-owners.ts` (`ownersFromUsers`), `server/auth/stage-owners.ts`
- **Production readiness** — partial — Tiers 0–3 done (storage/Docker/optimistic-lock/tests/CI+hardening); CI/CD live 2026.05.29; auto-deploy built but **unpushed**. → `guides/deployment.md`, `decisions/0001`, `decisions/0002`

## In flight

See active plans in `docs/planning/` (active-only).

## Live limitations / not yet built

- **Workflow engine — command-sourced rebuild + Stage-2 RM-coordination — shipped, e2e green 21/21.** Every workflow-state change goes through `dispatch(cmd)` → `decide()` → `applyDecision()` (`lib/workflow-engine.ts` + `lib/stage-action.ts`); `applyTransition`/`advanceOnDualSignOff` removed (invariant #1, one write seam). `WorkflowSnapshot` is a derived projection (⚠️ persistence pending — next bullet). **Commands:** `Transition`, `DualSignOff`, `SystemTransition`, `ReviseProposal`, `Withdraw`, `RegenerateMuap`, `MentionUser`, **`RollbackDocument`** (ADR-0008). **Stage 2 is RM-coordinated (ADR-0007):** RM collects docs + pulls SLIK/Pefindo (bureau data) + dispatches **Analisa Yuridis** (legal) + **Penilaian internal/KJPP** (appraisal) to the one **"Legal, Agunan & Biro"** stage / "Legal & Appraisal" role — both **tracked-not-gating**; the **2→3 advance fires on RM's SLIK handoff** (`stage2RmDataReady`), and the gate **relocates to MUAP→Risk** (`legalAppraisalComplete` blocks the MUAP-ladder request, e2e-proven). **RA is never a Stage-2 actor.** **Document versioning (ADR-0008, supersedes 0006):** `DocumentVersion` ledger + Drive `files.copy` snapshots at milestones (stage transition / regenerate / revise / rollback); rollback snapshots-current-first (no loss), pre-Komite, desk-gated `/docs/rollback` + read-only `/docs/versions` + DocsPanel "Riwayat versi". **Detail-app UI:** Kepatuhan (AML) + Penilaian Agunan on the **Data** tab; Documents shows the **Analisa Yuridis** tracked deliverable (no gate banner); SLIK/Pefindo are normal `ownerDeskForDocType`-gated rows + arbitrary-upload with an editable friendly name; **Pengajuan** proses step at stage 1; hard-gate tiles show pass/breach + threshold; Diskusi in a DossierSection card. **Snapshot persistence: Phase 3a shipped (next bullet)** — `stage` Int retirement (Phase 3b) + the cosmetic 6→4/1→16 renumber stay deferred. W1 ratifies finer config (BWMP tiers, Komite quorum, DPS scope, per-desk SLA). → `decisions/0007-stage2-rm-coordinated-origination.md`, `decisions/0008-document-versioning-snapshot-copies.md`, `references/hijra-bank-sop-digest.md`
- **Workflow engine — authoritative snapshot persistence (ADR-0004 §3) — Phase 3a SHIPPED 2026.06.09; Phase 3b pending.** The named `WorkflowSnapshot` (phase/step/status/closeReason) is now **persisted** on `Application` (`workflowSnapshot` JSONB), written at the single write seam (`server/repo/write.ts`) atomically under the optimistic guard and kept `== deriveWorkflowSnapshot(app)` (`lib/workflow.ts`; a null column re-derives). **`stage` Int remains the SSOT** — Phase 3b (invert authority so `stage` derives *from* the snapshot + migrate the ~150 raw `app.stage` readers onto predicates) is the irreversible, highest-blast-radius step, **stopped at the safe partial** pending human review of that diff (no live split-truth today: single SSOT = `stage`). The 6→4/1→16 renumber stays deferred-indefinitely. → `planning/workflow-snapshot-persistence.md`
- **Document system (MUAP/RSK + MoM/SP3)** — **V3 complete (ADR-0013) + V3.5 live (Batch 4, 2026.06.10)** — leak-proof `replaceAllText` fill is the active `createApplicationDocs` path; V1 (`buildFactMap`/NamedRange writer) + orphaned `seed-v2` deleted; `seed.ts` is the single module. **V3.5** adds targeted NamedRange fill for the MUAP master's underscore slots that `replaceAllText` cannot anchor — plafond/tenor (at creation) + No. MUAP/Tanggal (at the finalize re-fill once the MUAP ladder is fully signed); `[bracket]` slots stay on the V3 path. → `designs/doc-fill-v3.5-namedrange.md` MUAP/RSK `[bracket]` masters authored (core facts) + all 7 QR signature anchors created (`stampSignatureQr` live); MoM/SP3 `{{token}}` masters rebuilt from the real reference docs (denylist-clean) + env repointed. **Read-back = Markdown→AI (P1 shipped 2026.06.09):** `syncExtractionFromMarkdown` (`server/ai/extract-from-markdown{,-core}.ts`, masked+audited Vertex+Zod producing the same `ExtractedSnapshot`) is the **active** read-back — auto-on-Komite-entry (`ensureExtractionOnAdvance`) + the manual refresh route; consumers (5C+1S scoring, AI `snapshotBlock`) unchanged. The NamedRange/sentinel path (`extractApplicationDocs`/`syncApplicationDocs`, `server/google/extract/*`) stays **dormant** pending the **P2** parity-gated deletion (needs live Google-Doc creds — deferred). The deep-research agent (`server/research/*`) is built. → `designs/document-system.md`, `references/document-templates.md`
- **Doc access — auto-granted just-in-time (ADR-0014)** — done 2026.06.08 — per-app MUAP/RSK Docs are owned by the dedicated Mizan account and start PRIVATE, so every human previously hit Google's "request access" wall (the reported MUAP-drafting bug). `GET /api/applications/[id]/docs` (doc-panel mount) now calls `ensureDocAccessForActor` (`server/docs/access.ts`) → shares the Doc **per-email** at the role `lib/auth/doc-access.ts` `driveRoleForDoc` decides (writer=maker / reader=participant; shared with `MUAPTab`/`RSKTab`). Superadmin-impersonation grants the **real superadmin's** email (browser identity), not the `desk:` persona. Persisted/idempotent/upgrade-only in `DocAccessGrant`; never link-shared (PII). → `decisions/0014-doc-access-jit-per-user-grants.md`
- **AI assist** — design 2026.06.04 (`designs/ai-assist.md`): AI **advisory-only** (white-box · human-confirms · **never gating** · **never frozen** · masked+audited) at RM-MUAP (5C+1S draft · gap · **counter-offer**), Risk (`aiRiskAdvisory`), Bureau (`bureauSummary`), +Komite/Appraisal/SP3 optional. Vercel AI SDK + Zod on Vertex. Doc-creation: MUAP RM-invoke · RSK auto-on-Risk · SP3 approved-auto/conditional-invoke · MoM invoke (no-AI). → `designs/ai-assist.md`
- **Realtime notifications** — **V1 = polling** (notifications derived, `lib/notifications.ts`); SSE + Postgres LISTEN/NOTIFY **deferred** (design agreed). → `planning/realtime-notifications-sse.md`
- **AI audit = fail-open (best-effort)** — decided 2026.06.08 (human), **code aligned 2026.06.09** — a failed `recordAiInteraction` write logs but **never blocks or discards** the AI output: audit loss is an infra problem and a transient DB hiccup must not deny the user a generation (mirrors the PII-residual fail-open default; reversible code policy, no env toggle, no ADR). **All 5 AI-egress surfaces now fail-open:** `bureau` / `narrative` / `research` (inline try/catch) + `assistant` / `advisory` (via the shared `auditBestEffort` seam, `server/ai/audit-best-effort.ts`, unit-tested). Revisit before real customer data (G5 / OJK defensibility). → `server/ai/audit.ts`, `server/ai/audit-best-effort.ts`
- **Error monitoring** — owner: human. **Clean-host smoke** — remaining before deploy. → `guides/launch-gates.md`
- **Akad document generation — out of scope (V1).** The Stage-6 disbursement *workflow* is built (the "Proses Akad" step), but Mizan does **not** generate the akad contract document — it generates MUAP/RSK/MoM/SP3 only; the akad is authored/signed outside Mizan. `AkadBadge` is just a contract-type badge. → `references/scope-v1.md`
- **SP3→Akad post-Komite chain — out of scope (V1)** (follows the akad decision): the Stage-6 `DisbursementStatus` sub-states don't model SP3→akad→pencairan as distinct signed steps; the future-target shape is `designs/workflow-target.md`. → `references/scope-v1.md`
- **Minor hardening (BUILD-STATE digest)** — `enteredStageAt` invariant not validated (service-layer); gap-flagger run persistence is ephemeral UI state; send-back paths (2→1, 3→1) code/smoke-verified, not live-exercised.
- **Brainstorm merge** — done 2026.06.03: discovery/requirements docs imported to `references/` (+ read-only `sources/`) and `designs/` (pii-masking, admin-config-layer, workflow-target); GLOSSARY extended; newer brainstorm design (DPS-always, SP3 chain, maker-checker) folded in; 6→4 gate opened. **Two-session collaboration retired 2026.06.05 — this repo is the single source of truth; the `../brainstorm/` repo is historical (provenance markers cite it).** → `references/`
