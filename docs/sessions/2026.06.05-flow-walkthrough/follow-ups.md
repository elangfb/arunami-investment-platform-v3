# Follow-ups (non-active candidates)

The full finding list (F1–…, plus the deferred D-items) lives in `README.md` (sibling); the as-built
flow + AI provenance in `current-flow.md` (sibling). Findings are tracked at their canonical homes:

- **Open UI/UX findings** (e.g. ScoreOverview "AI menyusun draf skor" copy, wider `HardGateTile` adoption) → `../../planning/ui-ux-findings.md`.
- **Tooling gotchas** (emulator account-chooser native-click, agent-browser `eval` IIFE, persona switching via logout) → `../../MEMORY.md` (2026.06.05 entries).
- **Shipped flow / engine facts** → `../../CURRENT-STATE.md`.
- **Deferred workflow items** → `../../designs/workflow-finetune.md`.
